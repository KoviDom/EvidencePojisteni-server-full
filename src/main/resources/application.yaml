spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/evidence_pojistenych_db
    username: Kovidom
    password: surprise_secret
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true